% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_to_reference.R
\name{compare_to_reference}
\alias{compare_to_reference}
\title{Compare treatment groups to a reference group using influence curve-based SEs}
\usage{
compare_to_reference(reference_group, ...)
}
\arguments{
\item{reference_group}{The name of the reference group (must match `treatment_name
in the input groups).}

\item{...}{One or more group objects (output from \code{estimate_effects}) to
compare against the reference group. Each group must have a \code{treatment_name}, \code{ic} with \code{ic} and \code{time_horizons}, and \code{results} with \code{time_horizon} and \code{estimate}.}
}
\value{
A data.table with columns: \code{treatment}, \code{reference_group}, \code{ time_horizon}, \code{estimate} (difference in estimates), \code{se} (standard error), \code{lower} and \code{upper} (95\% confidence interval bounds).
}
\description{
This function takes a reference group and one or more treatment groups, and computes the difference in estimates between each treatment group and the reference group, along with standard errors and confidence intervals based on the influence curve (IC) differences.
}
\details{
The function computes the standard error of the difference in estimates between each treatment group and the
reference group by calculating the standard deviation of the difference in influence curves (ICs) divided by the square root of the sample size. It assumes that the ICs for the treatment and reference groups are aligned by time horizon and have the same length.
}
